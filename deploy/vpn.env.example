# Copy to /etc/contrai/vpn.env (root, mode 600). The tunnel itself is the
# provider's WireGuard file, mounted at /gluetun/wireguard/wg0.conf; its
# Endpoint must be an IP address, not a host name.
VPN_SERVICE_PROVIDER=custom
VPN_TYPE=wireguard
TZ=Europe/Paris
