# Virtual uv workspace — members live under packages/.
# Each member declares its own [project] table and dependencies.
[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
contrai-core = { workspace = true }
