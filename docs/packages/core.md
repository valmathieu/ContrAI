# contrai-core

Shared domain model for the ContrAI monorepo.

**Status:** empty — populated in phase 2. Will contain the model classes currently in `contrai-engine` (`Card`, `Deck`, `Hand`, `Bid`, `Contract`, `Trick`, `Round`, `Team`, `Player`, exceptions).

Consumed by `contrai-engine`, `contrai-analyzer`, and `contrai-scraper`.
