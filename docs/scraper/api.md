# contrai-scraper — API reference

`contrai-scraper` currently ships as a pair of entry-point scripts (`main.py`,
`run.py`) under `packages/contrai-scraper/` and does not expose an importable
Python package yet.

A proper API reference will be generated here once the scraper grows a stable
`contrai_scraper` package under `src/`.

<!-- TODO: replace with mkdocstrings directives once a stable package exists -->
