[project]
name = "contrai-data"
version = "0.5.0"
description = "Game-record format for the ContrAI monorepo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["contrai-core"]

[project.optional-dependencies]
test = [
  "pytest>=8.4",
  "pytest-cov>=6.0"
]
