[project]
name = "contrai-engine"
version = "0.3.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
  "contrai-core",
  "rich>=13",
  "pyfiglet>=1.0",
]
[project.optional-dependencies]
test = [
  "pytest>=8.4",
  "pytest-cov>=6.0"
]

[project.scripts]
contrai = "contrai_engine.cli:main"
