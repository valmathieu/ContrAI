[project]
name = "contrai-core"
version = "0.4.0"
description = "Shared domain model for the ContrAI monorepo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
test = [
  "pytest>=8.4",
  "pytest-cov>=6.0"
]
